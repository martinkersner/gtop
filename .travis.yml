dist: trusty
language: cpp

compiler:
  - g++

install: export CXX="g++-5"

addons:
  apt:
    packages:
    - g++-5

os:
  - linux
  
notifications:
 email:
  on_success: never
  on_failure: always

script: make
